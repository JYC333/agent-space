# This is the .env template. Do NOT edit this file directly.
# On first run, scripts/start.sh copies it to AGENT_SPACE_HOME/<mode>/.env
# (e.g. ~/aspace/dev/.env). Edit that file — it is never stored in the repo.

# ── Optional — Google OAuth ───────────────────────────────────────────────────
# Create credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI to register: http://localhost:5173/api/v1/auth/google/callback

# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:5173/api/v1/auth/google/callback
# FRONTEND_URL=http://localhost:5173

# Session cookies expire after this many days (default: 30)
# SESSION_EXPIRE_DAYS=30

# ── Optional — Auth ───────────────────────────────────────────────────────────

# DEBUG=false                    # set true to auto-seed a dev API key on startup

# ── Optional — Defaults ───────────────────────────────────────────────────────

# DEFAULT_SPACE_ID=personal
# DEFAULT_USER_ID=default_user
# REFLECTOR_MODE=pattern     # or: llm (uses LLM to generate memory proposals)

# ── Optional — Sandbox concurrency ───────────────────────────────────────────

# MAX_CONCURRENT_DOCKER_RUNS=3   # max Docker containers running at the same time

# ── Data root ─────────────────────────────────────────────────────────────────
# Inside Docker, AGENT_SPACE_HOME is always /aspace (see docker-compose.*.yml).
# On the host, start.sh mounts AGENT_SPACE_HOME/<mode>/ to /aspace (e.g. ~/aspace/dev).
# Do not set AGENT_SPACE_HOME in this file unless you know you need an override;
# the container environment sets /aspace.

# ── Optional — System Evolution ────────────────────────────────────────────────
# Register SYSTEM_CORE_REPO_PATH as a system_core workspace in the owner's
# personal space on backend startup.
# WARNING: This workspace uses system_core policy — all code changes require
# code_patch proposals, explicit approval, and git worktree sandbox execution.

# ENABLE_SYSTEM_EVOLUTION=true   # set true to enable (default: false)
# SYSTEM_CORE_OWNER_EMAIL=       # email of the user who owns the personal space

# ── Backup ────────────────────────────────────────────────────────────────────
# BackupService runs automatically when BACKUP_ENABLED=true.
# Two-person dogfooding MUST enable this before the first write session.
# Default is false (safe for tests and unattended CI).
#
# Archives go to AGENT_SPACE_HOME/backups/ (one file per interval).
# Auto backups are pruned to BACKUP_RETENTION_COUNT; manual backups are never pruned.
#
# BACKUP_ENABLED=true                # REQUIRED for dogfooding
# BACKUP_INTERVAL_HOURS=24           # auto-backup interval (default: 24)
# BACKUP_RETENTION_COUNT=7           # auto backups to keep — 7 × 24h = 7 days (default: 7)
# BACKUP_INCLUDE_LOGS=false          # include logs/ in archive (default: false)
# BACKUP_ON_STARTUP=true             # run an immediate backup on startup (default: true)
# BACKUP_ROOT=                       # override backup dir (default: AGENT_SPACE_HOME/backups)

# ── Advanced path overrides (rarely needed) ───────────────────────────────────
# These override individual sub-directories within AGENT_SPACE_HOME.
# Leave unset to use the defaults derived from AGENT_SPACE_HOME.

# WORKSPACE_ROOT=~/aspace/workspaces
# SANDBOX_ROOT=~/aspace/sandboxes
# DATABASE_URL=sqlite:////home/you/aspace/db/agent_space.sqlite
