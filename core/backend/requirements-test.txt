# Test and development dependencies — not needed in the production backend image.
# Install with: pip install -r requirements.txt -r requirements-test.txt
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
testcontainers[postgresql]>=3.7.0
docker>=7.0.0
