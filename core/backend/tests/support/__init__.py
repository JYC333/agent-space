"""Test-only helpers (not part of the application package).

Reusable building blocks live in ``factories``, ``fixtures`` (pytest plugin),
``fake_runtime``, ``fake_provider``, ``assertions``, and ``ids``.
"""
