{
  "name": "@agent-space/protocol",
  "version": "0.0.0",
  "description": "Shared, framework-free wire contracts (DTOs, command + event envelopes) for agent-space TS consumers. Schemas and types only — no handlers, no transport, no authority. Python remains the sole authority.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^6.0.3",
    "vitest": "^3.2.6"
  }
}
