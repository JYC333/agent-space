import type { ProviderPresetDTO } from "@agent-space/protocol" with { "resolution-mode": "import" };

export type ProviderPreset = ProviderPresetDTO;

export type ProviderPresetMode = ProviderPreset["mode"];

