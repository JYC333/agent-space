{
  "name": "@agent-space/control-plane",
  "version": "0.0.0",
  "description": "Official client-facing TypeScript control plane for agent-space. Serves TS-owned control-plane routes and, via a temporary legacy proxy, forwards all other /api/v1/* requests to the Python backend, which remains the sole authority.",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@agent-space/protocol": "file:../packages/protocol",
    "fastify": "^5.2.0",
    "undici": "^6.21.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^6.0.3",
    "vitest": "^3.2.6"
  }
}
